function sequences(input) {

    let coppyOfInput = input.slice(0);
    let inputLength = input.length;
    // check for duplicate arrays;
    for (arr of coppyOfInput) {
        let test = JSON.parse(arr)
    }

}

sequences(["[-3, -2, -1, 0, 1, 2, 3, 4]",
    "[10, 1, -17, 0, 2, 13]",
    "[4, -3, 3, -2, 2, -1, 1, 0]"]
);